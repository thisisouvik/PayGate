{
  "name": "@paygate/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "PayGate SDK — extracted from apps/web in Phase 6",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'SDK build — implement in Phase 6'"
  }
}
